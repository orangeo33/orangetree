// function swap(a,b){

//     let rrr = a;
//     a = b;
//     b = rrr;
//     }

//     console.log();

//     /*
//     */
function win(a, b){
    if (a>b)
        return (a);
    else if
        (a == b)
    return(0);
    else 
    return b;
// }

function array[1, 2, 3, 4 ,5];
    let room = array.length(0);
    array.length(0) = array.length(4)
    array.length(4) = room;
    console.log

    동작 설계/ 문법적 이해_ 단순한 코드로 수 차례 연습
 
    (i=0,i++)



    00 00
    01
    02
    09
    10
    99
    00 01
    99 99

    function swapFirstAndLast(arr) {
    if(arr.length < 2){
        return arr;
    } else{
        const first =arr[0];
        arr[0] = arr[arr.length - 1];
        arr[arr.length -1] = first;
        return arr;
 dsdsdd`ddddsssssscccrerfrfeefwsfd1`bjhjj
    function swapFirstAndLast(arr){
        const first = arr[0];
        arr[0] = arr[arr.length - 1];
        arr[arr.length - 1] = first;
    return arr;
    }
    